#ifndef OPERACIONES_H
#define OPERACIONES_H

//Declaración de prototipos
int retiro(Cliente);
int deposito(Cliente);
void consultaSaldo(Cliente);
void consultaMovimientos(Cliente);
int cambioClave(Cliente);

#endif // OPERACIONES_H
